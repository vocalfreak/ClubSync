#!/usr/bin/env ruby

if Gem.win_platform?
  exec "foreman start -f Procfile.dev"
else
  system("gem install foreman") unless `gem list foreman -i`.strip == "true"

  exec "foreman start -f Procfile.dev"
end
